export enum ControlPosition { absolute = 0, fixed = 1, normal = 2 }

export enum ControlAlignment { left = 'left', right = 'right', center = 'center' }

export enum IODataTypes {
    None = 'None',
    Scalar = 'Scalar',
    Array = 'Array',
    DataTable = 'Data Table',
    Redirect = 'Redirect',
    Boolean = 'Boolean'
}